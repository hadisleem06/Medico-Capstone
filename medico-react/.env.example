# Base URL for the future backend / AI API (built by teammates).
# While empty, the app runs entirely on in-memory mock data
# (see src/data/* and the service seam in src/api/*).
# Example once the backend exists:  VITE_API_BASE_URL=http://localhost:8080/api
VITE_API_BASE_URL=
